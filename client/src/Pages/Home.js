import React from 'react';
import Navigation from '../Components/Navbar';
import '../App.css';

const Home = () => {
    return (
        <div className="main-wrapper">
            < Navigation/>
        </div>
    );
};

export default Home;